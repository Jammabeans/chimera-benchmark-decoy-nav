{
  "name": "chimera-benchmark-decoy-nav",
  "version": "0.1.0",
  "private": true,
  "description": "External Chimera benchmark scaffold for the decoy-nav weakness category.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
